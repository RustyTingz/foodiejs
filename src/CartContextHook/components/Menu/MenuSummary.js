import classes from './MenuSummary.module.css';

const MenuSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>Foodie, the hub for delicious Food</h2>
      <p>
        Choose your favorites from the menu and enjoy a delicious selection of food for a meal at home.
      </p>
    </section>
  );
};

export default MenuSummary;
