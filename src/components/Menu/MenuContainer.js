import Menu from "./Menu";
import MenuSummary from "./MenuSummary";

const MenuContainer = () => {
  return (
    <div>
      <MenuSummary />
      <Menu />
    </div>
  );
};

export default MenuContainer;
